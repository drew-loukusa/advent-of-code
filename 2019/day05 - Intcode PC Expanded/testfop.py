
fop = "11102"
op = fop[len(fop)-2:]
fop = fop[:len(fop)-2]

a,b,c = 0,0,0
fop = list(fop)
reversed(fop)

if len(fop) > 0: a = fop.pop()
if len(fop) > 0: b = fop.pop()
if len(fop) > 0: c = fop.pop()






